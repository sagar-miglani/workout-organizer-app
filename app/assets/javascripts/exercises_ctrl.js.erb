(function() {
  "use strict";

  angular.module("app").controller('exercisesCtrl', function($scope, $route, $http){

    $scope.setUpIndex = function(){ 
      $route.reload();
      
      $http.get('/api/v1/exercises.json').then(function(response){
        $scope.exercises = response.data;
        initMap($scope.meetings);
      });
      // $http.get('/api/v1/tags.json').then(function(response){
      //   $scope.tags = response.data;
      // });
    };



    window.scope = $scope;

  });
}());